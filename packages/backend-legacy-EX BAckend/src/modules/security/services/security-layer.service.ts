/**
 * Security Layer Service - REEXPORTADO desde @agento/agent-core
 *
 * La única fuente de verdad está en @agento/agent-core/src/security
 * Este archivo solo reexporta para compatibilidad con imports existentes.
 */

export {
  SecurityLayerService,
  securityLayer,
  ExecutionMode,
  FULL_MODE_ALLOWED_TOOLS,
  LIMITED_MODE_ALLOWED_TOOLS,
  LIMITED_MODE_BLOCKED_TOOLS,
} from '@agento/agent-core';
